apiVersion: patronus/v2
family: artifact
type: skill
role: orchestration
name: subagent-driven-development
description: "Upstream-compatible alternative, not the default. `plan-execute` is the skill to use for executing a plan; its `sdd` mode runs this discipline when the plan warrants it. This is the superpowers `subagent-driven-development` skill kept verbatim to upstream: a fresh implementer subagent per task, a spec-and-quality review after each, one broad whole-branch review at the end, applied unconditionally rather than gated. Use it only to stay aligned with upstream, or to run every task under per-task review regardless of what the plan costs."
version: 1.2.0
entry: SKILL.md
files: [implementer-prompt.md, task-reviewer-prompt.md, scripts]
targets: [claude, codex, opencode]
defaults:
  scope: project
attribution:
  upstream: github.com/obra/superpowers
  license: MIT
  copyright: "Copyright (c) 2025 Jesse Vincent"
  commit: 896224c4b1879920ab573417e68fd51d2ccc9072
  note: "Vendored from the upstream `subagent-driven-development` skill (skills/subagent-driven-development/), including implementer-prompt.md, task-reviewer-prompt.md, and the scripts/ helpers (task-brief, review-package, sdd-workspace — audited: git-read + file-write only, no network/destructive ops). SKILL.md body is upstream-verbatim; a short Patronus note prepended at the top maps the `superpowers:` sibling cross-refs to Patronus artifacts and marks the un-vendored siblings (requesting-code-review, finishing-a-development-branch, using-git-worktrees) optional."
