apiVersion: patronus/v2
family: profile
role: lifecycle                            # L11 — profiles are lifecycle by nature
name: golang
summary: "Go project environment: the full core spine PLUS idiomatic Go conventions (Uber Go Style Guide, distilled). core + Go guidelines."
# golang = core, extended. `extends: core` pulls in every core layer (L1 spine +
# diagram-explain, L2 skills, L4 design-vocab + Context7/Serena, L5 github, ai-memory);
# this profile only ADDS the Go-idiomatic instruction overlay. List slots append, so
# go-style-uber joins core's instructions rather than replacing them.
extends: core
layers:
  instructions:
    - go-style-uber                        # idiomatic Go conventions distilled from the Uber Go Style Guide
todo:
  - "Source a Go eval (go test/vet + golangci-lint loop) when L8 lands (P7.5)."
