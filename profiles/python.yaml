apiVersion: patronus/v2
family: profile
role: lifecycle                            # L11 — profiles are lifecycle by nature
name: python
summary: "STUB — Python project agent environment. Items to be sourced (see todo)."
status: stub
layers:
  instructions: [agent-principles]         # generic agent house rules; TODO: + Python-specific conventions
  capabilities: [team-research@claude, team-implement@claude]   # Claude-flavoured; codex variant separate
  memory: memory-ai-memory
  # context / eval / observability / guardrails / sandbox: TODO — sourced later
todo:
  - "Source Python house-style + conventions instructions from <upstream-TBD>."
  - "Source Python eval (pytest/ruff/mypy loop) from <upstream-TBD>."
